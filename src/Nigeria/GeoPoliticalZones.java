package Nigeria;

import java.util.Arrays;
import java.util.Scanner;

public class GeoPoliticalZones {
    static Scanner input = new Scanner(System.in);

    public static void main(String[] args) {
        System.out.println("Enter Your State: ");
        String state = input.nextLine();

        for (GeoPoliticalZone zone : GeoPoliticalZone.values()){
           // if (Arrays.asList(GeoPoliticalZone.getStates());
                    //System.out.println(GeoPoliticalZone);
        }
//        }
       // if (state.equals("Benue") || state.equals("FCT") || state.equals("Kogi") || state.equals("Kwara") || state.equals("Nasarawa") || state.equals("Niger") || state.equals("Plateau")) {
         //    System.out.println(GeoPoliticalZone.NORTH_CENTRAL);
        //}


    }
}
